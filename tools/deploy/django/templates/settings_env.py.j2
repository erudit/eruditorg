import os

ALLOWED_HOSTS = ['{{ hostname }}']

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DEBUG = {{ debug_mode }}
COMPRESS_OFFLINE = not {{ debug_mode }}

ADMIN_URL = '{{ admin_url }}'

SECRET_KEY = '{{ secret_key }}'

GRAPPELLI_ADMIN_TITLE = '{{ verbose_project_name }} - {{ project_env }}'

DATABASES = {
    'default': {
    'ENGINE': 'django.db.backends.postgresql_psycopg2', # Add 'postgresql_psycopg2', 'mysql', 'sqlite3' or 'oracle'.
    'NAME': '{{ database_name }}',                      # Or path to database file if using sqlite3.
    'USER': '{{ database_user }}',
    'PASSWORD': '{{ database_password }}',
    'HOST': 'localhost',
    }
}

RENEWAL_FROM_EMAIL = 'client@erudit.org'
DEFAULT_FROM_EMAIL = 'contact@erudit.org'

STATIC_ROOT = "{{ project_root }}/static"
MEDIA_ROOT = "{{ project_root }}/media"

EMAIL_HOST = "{{ email_host }}"

# Raven settings
RAVEN_CONFIG = {
    'dsn': '{{ sentry_dsn }}',
}

victor_username = "{{ victor_username }}"
victor_password = "{{ victor_password }}"
victor_url = "{{ victor_url }}"
