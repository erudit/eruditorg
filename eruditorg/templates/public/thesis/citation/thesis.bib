@phdthesis{{ '{' }}{{ thesis.localidentifier }},
  title="{{ thesis.title|safe }}",
  author="{{ thesis.authors_display|safe }}",
  school="{{ thesis.collection_display|safe }}",
  url="{{ thesis.url }}",
  year="{{ thesis.publication_year }}"
}
